# 演習1-c01-2

(作問：COKO(kalme))

## 演習c0Xとは?

動画で扱ったクラスを、そのまま拡張する演習問題ダヨ。

## Problem - 名前を覚えテ！

なぞのいきものに名前をつけてあげヨウ。  
`<string>`をインクルードすると、文字列`string`型を使えるヨ。  
main.cppは私が書いたのを使ってネ。  

### 与えられるファイル

??? info "Nazo.h"

	```c++ linenums="1"
	#ifndef _NAZO_H_
	#define _NAZO_H_

	class Nazo {
	public:
		float height;
		int age;
		bool alive;

		void sayHi(void );
	};

	#endif /* _NAZO_H_ */
	```

??? info "Nazo.cpp"

	```c++ linenums="1"
	// 変えなくて大丈夫ダヨ！
	#include "Nazo.h"
	#include <iostream>

	using namespace std;

	void Nazo::sayHi(void )
	{
		cout << "Hi!" << endl;
		return;
	}
	```

??? info "main.cpp"

	```c++ linenums="1"
	// 変えずに使ってネ！
	#include <iostream>
	#include "Nazo.h"

	using namespace std;

	int main(void ){
		Nazo foo;
		foo.height = 13.5;
		foo.age = 5;
		foo.alive = true;
		foo.name = "フー君"; // メンバ変数の名前はnameがいいナー

		cout << "foo: " << endl;
		cout << "  height: " << foo.height << endl;
		cout << "  age: " << foo.age << endl;
		cout << "  name: " << foo.name << endl;
		return 0;
	}
	```

## 提出

コードを編集して、「提出」を押したらチェックするヨ。

--8<-- "docs/Exercises/ex1/ex1-c01-2/injection.html"

--8<-- "docs/includes/editor.html"
